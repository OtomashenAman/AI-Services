    
# version: "3.9"

# services:
#   fastapi_app:
#     image: ghcr.io/siddhanta2898/ai-services:latest  # ✅ Point to GHCR image
#     build: .
#     container_name: fastapi_app
#     ports:
#       - "8012:8012"
#     volumes:
#       - .:/app
#     env_file:
#       - .env
#     command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8012"]

# version: "3.9"

services:
  fastapi_app:
    image: ghcr.io/siddhanta2898/ai-services:latest
    container_name: fastapi_app
    ports:
      - "8012:8012"
    env_file:
      - .env
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8012"]
